export const siteConfig = {
  name: "CareerFlow",
  description:
    "Manage job applications, interviews, documents, and follow-ups in one organized workspace.",
  url: process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000",
} as const;
